import * as _mp from "core/common/mapStrings"

export default class EcDesignWgModelCntrl {
    static controllerId = 'app.designer.wgModel';
    static $inject = [];

    constructor() { }

    activate(): void {
        const that = this;


    }
}