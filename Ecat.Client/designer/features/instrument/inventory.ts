import * as _mp from "core/common/mapStrings"

export default class EcDesignInstvCntrl {
    static controllerId = 'app.designer.wgInvent';
    static $inject = [];

    constructor() { }

    activate(): void {
        const that = this;


    }
}